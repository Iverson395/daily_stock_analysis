import akshare as ak
import google.generativeai as genai
from tavily import TavilyClient
import sys
import os

# --------------------------  复用你已有的配置，完全不用改  --------------------------
genai.configure(api_key=os.getenv("GEMINI_API_KEY"))
ai_model = genai.GenerativeModel("gemini-1.5-flash")
tavily = TavilyClient(api_key=os.getenv("TAVILY_API_KEY"))
stock_code = sys.argv[1]
code = stock_code.split(".")[0]
# -------------------------------------------------------------------------------------

try:
    # --------------------------  第一步：稳定获取股票行情数据（核心修复）  --------------------------
    print("正在获取股票行情数据...")
    # 1. 拉取全市场最新行情，做严格的股票匹配
    spot_data = ak.stock_zh_a_spot_em()
    # 严格匹配股票代码，先判断有没有找到
    match_result = spot_data[spot_data["代码"] == code]
    if match_result.empty:
        raise Exception(f"未找到股票代码{stock_code}对应的股票，请核对代码是否正确")
    
    # 匹配成功，提取股票信息
    stock_info = match_result.iloc[0]
    stock_name = stock_info.get("名称", "未知股票")

    # 2. 容错提取所有核心数据，找不到的字段用「-」代替，不会再报错
    def safe_get(key, default="-"):
        return stock_info.get(key, default)

    # 整理核心行情数据（适配最新接口字段，加全容错）
    core_data = f"""
【{stock_name}（{stock_code}）今日核心行情】
最新价格：{safe_get('最新价')} 元
今日涨跌幅：{safe_get('涨跌幅')} %
开盘价：{safe_get('今开', safe_get('开盘'))} 元
最高价：{safe_get('最高')} 元
最低价：{safe_get('最低')} 元
成交量：{round(float(safe_get('成交量', 0))/10000, 2) if safe_get('成交量', 0) != '-' else '-'} 万手
成交额：{round(float(safe_get('成交额', 0))/100000000, 2) if safe_get('成交额', 0) != '-' else '-'} 亿元
换手率：{safe_get('换手率')} %
动态市盈率：{safe_get('市盈率-动态')}
市净率：{safe_get('市净率')}
今日涨速：{safe_get('涨速')} %
5分钟涨跌：{safe_get('5分钟涨跌')} %
年初至今涨跌幅：{safe_get('年初至今涨跌幅')} %
"""
    # ---------------------------------------------------------------------------------------------

    # --------------------------  第二步：Tavily实时新闻搜索（完全不变）  --------------------------
    print(f"正在搜索{stock_name}最新相关信息...")
    try:
        search_result = tavily.search(
            query=f"A股{stock_name} {code} 最新公告 新闻 行业政策 市场消息",
            search_depth="basic",
            max_results=3,
            include_answer=True
        )
        news_content = "【最新相关动态】\n"
        if search_result.get("results"):
            for idx, item in enumerate(search_result["results"][:3]):
                news_content += f"{idx+1}. {item['title']}\n摘要：{item['content'][:100]}...\n"
        else:
            news_content += "暂无最新重大公告或新闻\n"
    except Exception as news_error:
        news_content = f"【最新相关动态】新闻获取失败：{str(news_error)}\n"
    # ---------------------------------------------------------------------------------------------

    # --------------------------  第三步：Gemini深度分析（完全不变）  --------------------------
    print("正在生成深度分析报告...")
    prompt = f"""
你是一名拥有10年经验的A股专业投资顾问，基于下面的股票行情数据和最新动态信息，生成一份400字以内的专业深度分析报告，严格遵守以下要求：
1.  开头先给一个明确的今日表现总结，直接说涨跌核心原因
2.  分3个模块：盘面解读、消息面影响、操作建议，每个模块用小标题区分
3.  盘面解读结合行情数据，消息面结合搜索到的最新动态，不要泛泛而谈
4.  操作建议必须保守稳健，分持仓和空仓两种情况给出，不要给激进的买卖建议
5.  结尾必须加通用的股市风险提示
6.  语言口语化，通俗易懂，适合普通散户投资者，不要用太晦涩的专业术语

【基础行情数据】
{core_data}

【最新相关动态】
{news_content}
    """
    ai_response = ai_model.generate_content(prompt)
    final_report = f"📈 {stock_name} 深度分析报告\n\n{ai_response.text}\n\n{core_data}\n{news_content}"

except Exception as e:
    # 全链路异常提示，精准告诉你哪里错了
    final_report = f"❌ 深度分析失败\n股票代码：{stock_code}\n错误原因：{str(e)}\n\n排查建议：\n1. 确认股票代码格式正确（沪市加.SH 深市加.SZ，例：601777.SH）\n2. 确认股票是正常交易的A股，没有停牌/退市\n3. 核对Gemini、Tavily密钥名称是否正确，API额度是否充足"

# 保存报告供钉钉推送
with open("result.txt", "w", encoding="utf-8") as f:
    f.write(final_report)

print(final_report)
